# Yui Portfolio（portfolio2）スマホ版ブラッシュアップ指示書

作成日：2026-09-02
対象：`index.html` のスマホ版（767px以下）レイアウト・演出のブラッシュアップ

## この指示書について

ご依頼いただいた6項目を実際のコード（`index.html` / `css/*.css` / `js/*.js`）と照らし合わせて確認しました。
このうち **単純なCSS数値の調整で完結し、見た目の副作用が小さいもの** はCowork側で先に直接修正・反映済みです。
残りの項目は、**実機・ブラウザで実際の見た目を確認しながら座標や数値を追い込む必要がある**（＝コードを読むだけでは正しい値を断定できない）ため、Claude Code側での対応をおすすめします。

---

## ✅ Cowork側で対応済み（`css/style.css` に反映・コミット済み）

### 対応済み-1：アーチ窓をスマホでカット
- ご依頼：「アーチ窓は幅的に採用が難しいと思うので、カットで」
- 対応：`css/style.css` の `@media (max-width: 767px) { .hero { border-radius: ... } }` を `border-radius: 0` に変更し、スマホではHero上端のアーチ形状を完全に無くしました。
- 補足：`#main` 側のアーチ窓演出（内縁ハイライト・壁のマウス追従光など）はもともと767px以下では非表示済みだったため、今回の変更でPC/スマホの扱いが揃いました。

### 対応済み-2：セクションの左右余白を拡大（コンパクトに見せる）
- ご依頼：「各セクションの枠の幅が画面いっぱいに広がり、左右の余白がなく詰まっている」
- 原因：全セクション共通のレイアウト変数 `--section-pad-x`（`css/style.css`）が、スマホ基準で **18px** しかありませんでした。About・Skills・Works・Contactなど、ほぼ全セクションがこの1つの変数を参照しているため、ここが余白の正体でした。
- 対応：`--section-pad-x` を **18px → 24px** に変更しました。全セクション共通の値なので、この1箇所の変更で見た目全体が少し「シュッと」した印象になっているはずです。
- 実機で見て、まだ詰まって見える／逆に広すぎると感じた場合は、この変数の値だけを再調整すれば全セクションに反映されます（`css/style.css` 69行目付近）。

---

## 🔧 Claude Codeで対応をお願いしたい項目

いずれも「実際にスマホ幅で表示して確認しながら数値・座標を追い込む」タイプの作業です。

### 1. イントロをスマホでも流したい

**現状分析**
`js/intro.js` を確認したところ、PC限定でイントロを止めているコードは見当たりません。早期returnの条件は「`prefers-reduced-motion`」「2回目以降の訪問（sessionStorage）」「GSAP未読込」の3つのみで、画面幅による分岐はありません。`isMobile`という変数はありますが（104〜116行目）、これは瓶の開始位置やアニメーション時間を**スマホ用に調整するため**に使われているだけで、そもそも「スマホでも動く」前提で作られています。

**お願いしたいこと**
- 実機／DevToolsのスマホエミュレーションで実際に一連の流れ（瓶が流れ着く→クリックで開封→巻き紙→本編表示）が動くか確認してください。「流れていない」と感じる場合、コード上の分岐が原因ではなく、演出中の見た目（画像の切れ方・要素の重なり）に違和感があって止めている／見えていない可能性があります。
- 背景画像（`sozai/intro-bg-beach.png`、`sozai/intro-wave-foam.png`）や瓶・巻き紙の画像が、縦長のスマホ画面でも不自然にトリミングされないか確認してください。
- 瓶の開始位置・巻き紙の文字サイズなど、`js/intro.js` の `washBottleAshore()` 内の数値（`startX`/`startY`/`startScale`等）を実機を見ながら微調整してください。

### 2. ファーストビュー（FV）のPC画面内スクロール演出をスマホにも適用したい

**現状分析**
PC版のHeroでは、写真内のノートPC画面（`#heroLaptopMask`）に実績スクリーンショットを射影変換で嵌め込み、`js/hero.js` の `startLaptopScroll()` で中のスクリーンショットをスクロールさせています。
ただし、この仕組みは以下の理由でそのままスマホに持っていくことができません。

- `#heroLaptopMask` と見出しSVGは `css/responsive.css` で767px以下では `display: none !important` にされている
- そもそもスマホ専用写真（`sozai/hero-mobile.png`）にはノートPCが写っていない構図（縦長のデスク写真）

**方針を決めていただきたい点**
- **方針A**：スマホ用にも「スマホやノートPCの画面が写ったカット」の写真を新規に用意し、`js/hero.js` の射影変換の仕組みをスマホ版にも移植する。写真内の画面の4隅座標は、`index.html?calibrate` でアクセスすると出てくるキャリブレーションモード（`js/hero.js` 136行目〜）で実際に見ながら調整する前提の仕組みなので、実機で見ながらでないと座標が決められません。
- **方針B**：新規写真は用意せず、既存のスマホ写真の空いているスペースに簡易的な「スマホ画面フレーム」（画像、または角丸+枠のCSS）を重ねて、その中だけ実績画像をスクロールさせる簡易版にする。

どちらが良いか、見た目を確認しながらの判断が必要なため、Claude Code側での実装をおすすめします。

### 3. ヒーロー画像と本編の境目の波のズレ・境界線を修正したい

**現状分析**
Hero下端と次セクションの継ぎ目には `css/animation.css` の `.hero-seam-fade`（78〜106行目）というグラデーション波が重なっており、これが継ぎ目を自然に見せる役割を持っています。この要素は `position: absolute` + 固定の `top`（スマホは `-34px`）・`height: 200px` で配置されています。

一方で、Hero自体はスマホ専用に `aspect-ratio: 853 / 1844` という非常に縦長の比率（`css/responsive.css` 308行目）になっており、これはPC版（1536:1024の横長）向けに調整された `.hero-seam-fade` の重なり量・波の谷の深さをそのまま流用しています。**この比率のズレが「波がズレて境界線が見える」原因である可能性が高いです。**

また、Hero本体が持つ3層の波アニメーション（`.wave-layer-track`）は、現在 `css/animation.css` 47〜49行目のコメントにある通り「一時カット」で `display: none` になっています。

**お願いしたいこと**
- スマホ実機・エミュレータで実際のズレ具合を見ながら、`@media (max-width: 767px)` 内の `.hero-seam-fade` の `top` / `height`、および `index.html` 204〜224行目のSVG波の谷の深さ（`path`の数値）をスマホの縦長写真に合わせて再調整してください。
- あわせて、`#hero .wave-layer-track` の「一時カット」を復活させて境目に使うか、`.hero-seam-fade` だけで自然に見せる方針にするかも判断してください。

### 4. 「対応できること」表記が2行に折り返す・幅を取りすぎる問題

ご依頼の「対応できることのタグが幅を取りすぎなので、一行にして（今の2つずつの配列は崩さない）」に該当しそうな箇所が2つあったため、両方の現状を書きます。実機でどちらを指しているか（あるいは両方か）ご確認ください。

**(a) Hero上の水玉演出（できることピックアップ：HP制作／LP制作／デザイン／実装）**
`js/hero-splash.js` が波の上に動的に生成する水玉です。`css/animation.css` の `.hero-splash__label` は既に `white-space: nowrap` ＋ 16px固定で「1行に収まる」設計になっているため、**こちらは現状問題なさそうです**。

**(b) 「対応できること」セクション（id="skills"）のスキルカード9枚**
`css/responsive.css` 493〜524行目で、スマホは2列（`grid-template-columns: repeat(2, 1fr)`）・`font-size: 12.5px` に設定されています。「LP・ホームページ制作」「ヒアリング・構成整理」のような長い項目名は、2列の幅では1行に収まらず折り返っている可能性が高いです。**ご依頼の「対応できることのタグ」はおそらくこちらを指していると思われます。**

**対応案（2列レイアウトは維持したまま1行に収める方法）**
1. **文言を短縮する**（例：「LP・ホームページ制作」→「LP・HP制作」／「ヒアリング・構成整理」→「ヒアリング」／「HTML / CSS実装」→「HTML/CSS」）※表記変更になるため、実装前にYuiさんの確認をお願いします
2. `skill-card` を縦積み（アイコン→文字）から横並び（アイコン｜文字）に変更し、文字の横幅を広く確保する
3. `font-size` をさらに小さくする（可読性が落ちるため非推奨、最終手段）

実機で見た目を比較しながら、一番自然に見える方法を選んで実装してください。

---

## その他、気になった点（対応必須ではなく提案です）

- スマホ版のHero写真は `aspect-ratio: 853 / 1844`（縦横比 約1:2.16）とかなり縦長で、かつ `max-height` の上限指定がありません（`css/responsive.css` 312行目）。機種によってはHeroだけで画面の2〜3画面分の高さになり、本編にたどり着くまでのスクロールが長くなる可能性があります。`max-height: 100svh` 程度の上限を検討してもよいかもしれません。
- 今回スマホ版で変更した内容（アーチ窓カット・余白調整）を `README.md` の「デザイン上の注意点」に一言追記しておくと、今後の引き継ぎがスムーズです。

---

## 対象ファイル一覧

| ファイル | 内容 |
|---|---|
| `index.html` | Hero構造・波のSVG・スキルカード等のマークアップ |
| `css/style.css` | `--section-pad-x` / `.hero` のborder-radius（**対応済み**） |
| `css/responsive.css` | スマホ用ブレークポイント（767px）の各種上書き |
| `css/animation.css` | `.hero-seam-fade`・`.wave-layer-track`・`.hero-splash__label` |
| `js/hero.js` | ノートPC画面はめ込み・射影変換・キャリブレーションモード |
| `js/hero-splash.js` | 「できること」水玉の生成ロジック |
| `js/intro.js` | 読み込み時イントロ（瓶が流れ着く演出） |
